{
  "permissions": {
    "allow": [
      "Exec(ls)",
      "Exec(Get-ChildItem)",
      "Exec(Copy-Item)",
      "Exec(Test-NetConnection)",
      "Exec(curl)",
      "Exec(Invoke-WebRequest)",
      "Exec(New-Object)",
      "Exec(email=\"test@example.com\")",
      "Exec(password=\"TestPass123\"})",
      "Exec($genResponse.Content)",
      "Exec(($genResponse.Content)",
      "Exec(git status)",
      "Exec(git diff)",
      "Exec(git log)",
      "Exec(git add)",
      "Exec(git commit)"
    ]
  }
}